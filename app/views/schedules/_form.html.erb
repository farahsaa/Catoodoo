<%= form.hidden_field(:cat_id)%>
<%= form.select(:task_id, options_for_select(@tasks.map{|t| [t.title, t.id]}))%>
<%= form.select(:scheduled_at, options_for_select((0..72).map{|i|time = Time.now.utc + i.hours;[time.to_s, time.to_i]})) %>
